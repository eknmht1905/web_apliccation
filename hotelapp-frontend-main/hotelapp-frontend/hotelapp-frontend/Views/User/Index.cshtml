@model IEnumerable<hotelapp_frontend.Models.Usuario>

@{
    ViewData["Title"] = "Lista de Usuarios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (!Model.Any())
{
    <p>Kulanıcı Bulunamadı!</p>
}
else
{
<br />
<br />
<br />
<section class="rooms-section spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h3>Kullanıcı Özellikleri</h3>
                <div class="d-flex justify-content-end mb-3">
                    <a href="@Url.Action("Crear", "User")" class="btn btn-success">Ekle</a>
                </div>
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>İsim</th>
                            <th>Doğum Tarihi</th>
                            <th>E-Posta</th>
                            <th>Durum</th>
                            <th>Yetkisi</th>
                            <th>Haraketler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var usuario in Model)
                        {
                            <tr>
                                <td>@usuario.Nombre</td>
                                <td>@usuario.FechaNacimiento.ToShortDateString()</td>
                                <td>@usuario.Correo</td>
                                <td>@(usuario.Estado ? "Aktif" : "Pasif")</td>
                                <td>
                                    @if (usuario.IDRol == 1)
                                    {
                                        <span>Yönetici</span>
                                    }
                                    else if (usuario.IDRol == 2)
                                    {
                                        <span>Müşteri</span>
                                    }
                                </td>
                                <td>
                                    <a href="@Url.Action("Detalle", "User", new { id = usuario.IDUsuario })" class="btn btn-info">Detaylar</a>
                                    <a href="@Url.Action("Editar", "User", new { id = usuario.IDUsuario })" class="btn btn-warning">Düzenle</a>
                                    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal_@usuario.IDUsuario">Sil</button>

                                    <!-- Modal -->
                                    <div class="modal fade" id="deleteModal_@usuario.IDUsuario" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
                                        <div class="modal-dialog" role="document">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="deleteModalLabel">Silme Onayı</h5>
                                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                                        <span aria-hidden="true">&times;</span>
                                                    </button>
                                                </div>
                                                <div class="modal-body">
                                                        Bu kullanıcıyı silmek istediğinizden emin misiniz?: @usuario.Nombre?
                                                </div>
                                                <div class="modal-footer">
                                                    <a href="@Url.Action("Eliminar", "User", new { id = usuario.IDUsuario })" class="btn btn-danger">Sil</a>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>

}
